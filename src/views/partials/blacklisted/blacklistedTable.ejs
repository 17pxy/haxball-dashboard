<% if (blacklisted.length > 0) { %>
<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-10">
      <h1 class="text-center text-black">Lista De Blacklisteados</h1>
      <div class="table-responsive">
        <table class="table table-striped table-hover table-lg text-center">
          <thead>
            <tr class="table-dark">
              <th scope="col">Usuario</th>
              <th scope="col">Autenticador</th>
              <th scope="col">Conexion</th>
            </tr>
          </thead>
          <tbody>
            <% for (var i = 0; i < blacklisted.length; i++) { %>
            <tr>
              <td><%= blacklisted[i].name %></td>
              <td><%= blacklisted[i].auth %></td>
              <td><%= blacklisted[i].conexion %></td>
            </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<% } else { %>
<h1 class="text-center mt-4">
  <strong>No se encontraron estadisticas!</strong>
</h1>
<% } %>
